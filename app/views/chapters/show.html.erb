<p id="notice"><%= notice %></p>

<p>
  <strong>Position:</strong>
  <%= @chapter.position %>
</p>

<p>
  <strong>Title:</strong>
  <%= @chapter.title %>
</p>

<p>
  <strong>Theme:</strong>
  <%= @chapter.theme %>
</p>

<%= render partial: 'events/list', locals: { events: @chapter.events.default_order}  %>

<%= link_to '新建事件', new_event_path(chapter_id: @chapter.id) %>
<%= link_to 'Edit', edit_chapter_path(@chapter) %> |
<%= link_to 'Back', chapters_path %>
